apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tam-copilot-data
  namespace: tam-copilot
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
